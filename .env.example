# Non-secret config only. Real secrets live in ./secrets/*.txt (gitignored) — never here.
# Copy: cp .env.example .env  (prod: set strong values + create secrets files)
POSTGRES_DB=appdb
POSTGRES_USER=app
# POSTGRES_PASSWORD is NOT here on purpose — use secrets/postgres_password.txt
CORS_ORIGIN=http://localhost
NEXT_PUBLIC_API_URL=/api
